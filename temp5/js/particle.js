/**
 * Particleground demo
 * @author Jonathan Nicol - @mrjnicol
 */

$(document).ready(function() {
  $('body').particleground({
    dotColor: '#7F7F7F',
    lineColor: '#fff',
    lineWidth: '0',
    particleRadius: '0',
    parallaxMultiplier: '0'
  });
});